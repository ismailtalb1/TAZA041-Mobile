# حالة إصدار TAZA 041 للموبايل — النسخة الأكاديمية النهائية

هذه هي النسخة الأكاديمية النهائية المعتمدة لتطبيق الزبون Flutter. اكتمل فحص
المصدر والتبعيات وAndroid، ولا توجد أعمال برمجية إلزامية معلقة ضمن نطاق العرض
والتقييم المحلي.

## النطاق المعتمد

| المكوّن | القيمة |
| --- | --- |
| مشروع الموبايل | `C:\Users\DELL005\Desktop\taza041_flutter_customer_mobile` |
| Laravel API | `C:\Users\DELL005\Desktop\TAZA041 - Web & Dashboard` |
| هوية الحزمة | `com.taza041.customer.mobile` |
| إصدار التطبيق | `1.0.2+3` |

## نتيجة الفحص النهائي

- Flutter `3.41.7` وDart `3.11.5` على القناة المستقرة.
- Android SDK `36.0.0`، وJava 17، وجميع رخص Android مقبولة.
- نجح `dart analyze` دون أخطاء أو تحذيرات.
- نجح فحص التنسيق على 36 ملف Dart دون الحاجة إلى أي تعديل.
- نجحت 32 حالة اختبار تشمل API، التخزين، مسارات التوصيل، الولاء، اتجاه الهيدر،
  وجميع الشاشات بعروض 320 و360 و390 و430 بكسل.
- نجح بناء APK Debug عبر Gradle 8.14 وAndroid Gradle Plugin 8.11.1.
- ملف APK الأكاديمي موجود في `build/app/outputs/flutter-apk/app-debug.apk`.
- بصمة SHA-256 لآخر APK تم بناؤه:
  `CE2A6A744EEA3EDECF71F396A179F08A0EEBD94E09DE182BD03A98990504D6E1`.
- يعمل محاكي Android المحفوظ `Pixel_7_Pro`، وتظهر أدوات Android سليمة في
  `flutter doctor`.
- حُدّثت أربع تبعيات فرعية متوافقة داخل `pubspec.lock` دون تغيير قيود الحزم
  الرئيسية أو Flutter SDK أو Gradle.
- نجح فحص 149 حزمة Pub محلولة مقابل قاعدة OSV دون العثور على ثغرات معروفة.

## التحسينات المثبتة

- يدعم نموذج الزبون كتالوج مستويات الولاء، معامل كسب النقاط، التقدم للمستوى
  التالي، وعدد النقاط المتبقية.
- تُزامن إعدادات الولاء الحية القادمة من Laravel مع حساب الزبون.
- يحسب تقدير النقاط في التطبيق باستخدام معامل المستوى الفعلي.
- تعرض صفحة الملف الشخصي بطاقة كاملة لبرنامج الولاء بالعربية والإنكليزية، مع
  المستوى الحالي، المضاعف، شريط التقدم، وبقية المستويات.
- أضيفت اختبارات صريحة لحمولة الولاء وحساب المضاعف.

## ما تم الحفاظ عليه عمدًا

بناءً على اعتماد المشروع على المحاكي الخارجي، لم يُحذف أو يُنقل أي شيء من:

- `android/.gradle` أو Gradle Wrapper وإعدادات Android.
- `.dart_tool` أو `build` أو APK الحالي.
- Android SDK أو AVD أو إعدادات المحاكي `Pixel_7_Pro`.
- أصول Android وiOS والويب المولدة للأيقونات وشاشات البدء؛ تكرار محتواها مقصود
  لاختلاف المنصة والدقة.

## حدود النسخة الأكاديمية

- الدفع النقدي ونقاط الولاء ضمن نطاق العرض. سيريتل كاش وشام كاش معطلتان عمدًا
  لعدم وجود بوابة دفع تجارية.
- يستخدم التطبيق OpenStreetMap/OSRM للعرض الأكاديمي مع آلية احتياطية عند تعذر
  الاتصال، ولا يلزم مزود خرائط تجاري.
- لا تحتاج النسخة الأكاديمية إلى متجر تطبيقات أو مفتاح توقيع Release أو شهادات
  Apple أو رابط API عام عبر HTTPS.
- تنبيه Visual Studio في `flutter doctor` يخص بناء تطبيق Windows فقط ولا يؤثر على
  Android أو تطبيق الهاتف.
- تحذير Gradle عن ميزات قديمة يخص التوافق المستقبلي مع Gradle 9؛ السلسلة الحالية
  على Gradle 8.14 ناجحة ولم تُغيّر حفاظًا على بيئة المحاكي.

## إعادة التحقق

```bash
dart format --output=none --set-exit-if-changed lib test
dart analyze
flutter test --no-pub
flutter build apk --debug --no-pub
```

الحالة النهائية: **جاهز للتسليم والعرض الأكاديمي على Android**.
